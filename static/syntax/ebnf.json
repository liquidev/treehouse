{
    "patterns": [
        { "regex": "[a-zA-Z_][a-zA-Z0-9_]*", "is": "keyword2" },
        { "regex": "\"(\\\\\"|[^\"])*\"", "is": "string" },
        { "regex": "'('|[^'])*'", "is": "string" },
        { "regex": "[+*?=|,-]", "is": "operator" }
    ],
    "keywords": {
    }
}
